; vim:ft=dosini

[colors]
white = #FFFFFF
gray = #DEDEDE
black = #000000
pink = #F5A9B8
blue = #5BCEFA

foreground = ${colors.black}
background = ${colors.white}
dimmed = ${colors.gray}
warn = ${colors.pink}

[bar-base]
height = 30

offset-y = 25
offset-x = 25

padding = 5
module-margin = 2.5

fixed-center = true

line-size = 2.5

background = ${colors.background}
foreground = ${colors.foreground}

font-0 = Roboto:style=Regular:size=10;2
font-2 = FontAwesome5Free:style=Solid:size=8;2
font-1 = FontAwesome5Brands:style=Regular:size=8;2

modules-left = workspaces
modules-center = window

[bar/laptop]
inherit = bar-base

monitor = LVDS1

width = 1316

modules-right = wifi brightness battery volume date time

[bar/vga]
inherit = bar-base

monitor = VGA1

width = ${env:VGA_BAR_WIDTH:1860}

modules-right = wifi battery volume date time

[module/date]
type = internal/date

date = %A, %B %d

[module/time]
type = internal/date

time = %H:%M

label = %time%

[module/workspaces]
type = internal/bspwm

ws-icon-0 = discord;
ws-icon-1 = terminal;
ws-icon-2 = firefox;
ws-icon-3 = code;
ws-icon-4 = music;
ws-icon-default = 

label-focused = " %icon% "
label-focused-padding = 1
label-focused-underline = ${colors.foreground}

label-occupied = " %icon% "
label-occupied-padding = 1

label-empty = " %icon% "
label-empty-padding = 1
label-empty-foreground = ${colors.dimmed}

label-urgent = " %icon% "
label-urgent-padding = 1
label-urgent-background = ${colors.warn}

[module/brightness]
type = internal/xbacklight

format =   <label>

[module/window]
type = internal/xwindow

label-maxlen = 50

[module/battery]
type = internal/battery

battery = BAT0
adapter = AC

format-charging = <animation-charging>  <label-charging>

format-discharging = <ramp-capacity>  <label-discharging>

animation-charging-0 = 
animation-charging-1 = 
animation-charging-2 = 
animation-charging-3 = 
animation-charging-4 = 

ramp-capacity-0 = 
ramp-capacity-1 = 
ramp-capacity-2 = 
ramp-capacity-3 = 
ramp-capacity-4 = 

[module/wifi]
type = internal/network

interface = wlp3s0

format-connected =   <label-connected>

format-packetloss =   <label-packetloss>

format-disconnected =   <label-disconnected>

label-connected = %essid%

label-packetloss = %essid%

label-disconnected = Disconnected

[module/volume]
type = internal/pulseaudio

master-soundcard = default
speaker-soundcard = default
headphone-soundcard = default

master-mixer = Master
speaker-mixer = Speaker
headphone-mixer = Headphone

headphone-id = 19

mapped = true

format-volume = <ramp-volume>  <label-volume>

format-muted =   <label-muted>

ramp-volume-0 = 
ramp-volume-1 = 
ramp-volume-2 = 
